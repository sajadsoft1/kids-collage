<div direction="rtl">
این دقیقاً همان payloadی است که اگر مستقیماً template GeneralTemplate را صدا می‌زدی، به آن می‌دادی؛ پس کد خوانا و قابل پیش‌بینی است.اگر دوست داری، در مرحله بعد می‌توانیم برای استفاده‌های رایج (مثلاً ست کردن یک فیلد ساده با dot notation) چند helper خیلی کوچک بنویسیم (مثلاً setDot(SettingEnum::GENERAL, 'company.name', 'value')) که روی همین set() سوار شوند، بدون اینکه قلب ساختار را پیچیده کنند.
</div>